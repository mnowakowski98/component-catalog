import Component from '../component'

interface Props {
    id: string
    component: Component
    supply: {
        derp: string
    }
    electricalProperties: {
        derp: string
    }
}

export default async function ComponentPage() {
    return <div>
        
    </div>
}